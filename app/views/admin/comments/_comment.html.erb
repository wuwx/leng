<%= content_tag_for :tr, comment do %>
  <td><%= comment.id %></td>
  <td><%= comment.user.name %></td>
  <td><%= link_to "删除", [:admin, comment], :method => :delete, :class => "btn btn-small btn-danger" %></td>
<% end %>